# Copy to .env and fill in your key (same Groq account as Projects 1-3).
# .env is gitignored — never commit your real key.
GROQ_API_KEY=your_key_here

# Optional overrides:
# GROQ_MODEL=llama-3.3-70b-versatile
# PG_SUBMIT_LIMITS=10 per minute;100 per day
